<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.UTSLecture" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here if needed -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowElevation">4dp</item>

        <!-- Tambahkan style default untuk MaterialButton -->
        <item name="materialButtonStyle">@style/Widget.App.Button</item>
    </style>

    <style name="Theme.UTSLecture" parent="Base.Theme.UTSLecture" />

    <!-- Style untuk TextInputLayout -->
    <style name="text_input" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="android:layout_margin">8dp</item>
    </style>

    <!-- Tambahkan style khusus untuk MaterialButton -->
    <style name="Widget.App.Button" parent="Widget.Material3.Button">
        <item name="android:stateListAnimator">@null</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="elevation">6dp</item>
        <item name="strokeWidth">0dp</item>
    </style>
</resources>